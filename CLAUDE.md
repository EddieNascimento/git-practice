# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty (no commits, no source files). There is no
build system, test suite, or architecture to document yet.

Once code is added, re-run repository initialization (or update this file
directly) to fill in:
- Common commands (build, lint, test, including how to run a single test)
- High-level architecture and structure
